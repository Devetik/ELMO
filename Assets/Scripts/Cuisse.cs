using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cuisse : MonoBehaviour
{
    public GameObject cuisse; // Assurez-vous d'assigner cela dans l'inspecteur
    // Start is called before the first frame update
    // void Start()
    // {
    //     HingeJoint cuisseJoint = cuisse.GetComponent<HingeJoint>();
    //     // Maintenant, vous pouvez accéder aux propriétés du HingeJoint et les modifier
    //     // Par exemple, pour appliquer un couple (force de rotation) :
    //     var motor = cuisseJoint.motor;
    //     motor.force = 100;
    //     motor.targetVelocity = 90; // Vitesse désirée
    //     motor.freeSpin = false;
    //     cuisseJoint.motor = motor;
    //     cuisseJoint.useMotor = true; // Activez le moteur pour appliquer la force
    // }

    // // Update is called once per frame
    // void Update()
    // {
        
    // }
}
